{
  "name": "magic",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @magic/web dev",
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r typecheck",
    "lint": "oxlint packages",
    "assets": "tsx scripts/fetch-assets.ts",
    "cards:index": "tsx scripts/build-card-index.ts"
  },
  "devDependencies": {
    "@magic/renderer": "workspace:*",
    "@magic/shared": "workspace:*",
    "@types/node": "^22.5.4",
    "oxlint": "^1.71.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
